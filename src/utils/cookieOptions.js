/**
 * Secure cookie configuration options
 * These settings help protect cookies from various attacks
 */

import { cookieConfig } from '../config/security.js';

// Use the centralized cookie configuration
const cookieOptions = cookieConfig;

export default cookieOptions;